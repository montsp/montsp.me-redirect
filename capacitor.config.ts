import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'montsp.me-redirect',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
